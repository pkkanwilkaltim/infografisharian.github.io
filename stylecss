/* ============================================================
   Generator Laporan Harian Kanwil — Kaltim-Utara
   Versi: HTML Murni (tanpa PHP / server)
   ============================================================ */

*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background: #f0f4f8;
  color: #1a2332;
  min-height: 100vh;
}

/* ---- HEADER ---- */
.header {
  background: linear-gradient(135deg, #1a3a5c 0%, #0d2540 100%);
  color: white;
  padding: 16px 24px;
}
.header-inner {
  max-width: 860px;
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  flex-wrap: wrap;
}
.header h1 {
  font-size: 17px;
  font-weight: 600;
  letter-spacing: 0.3px;
}
.header p {
  font-size: 12px;
  opacity: 0.75;
  margin-top: 4px;
}
.header-date {
  font-size: 12px;
  background: rgba(255,255,255,0.12);
  padding: 5px 12px;
  border-radius: 20px;
  white-space: nowrap;
}

/* ---- CONTAINER ---- */
.container {
  max-width: 860px;
  margin: 0 auto;
  padding: 20px 16px 40px;
}

/* ---- CARDS ---- */
.card {
  background: white;
  border-radius: 10px;
  padding: 16px 18px;
  margin-bottom: 14px;
  box-shadow: 0 1px 4px rgba(0,0,0,0.07);
}
.card-title {
  font-size: 13px;
  font-weight: 600;
  color: #1a3a5c;
  margin-bottom: 12px;
  display: flex;
  align-items: center;
  gap: 7px;
}
.card-title .icon {
  width: 22px;
  height: 22px;
  background: #e8f0fa;
  border-radius: 5px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 12px;
}

/* ---- META GRIDS ---- */
.meta-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 10px;
}
@media (max-width: 600px) {
  .meta-grid { grid-template-columns: 1fr; }
}
.field label {
  font-size: 11px;
  color: #64748b;
  display: block;
  margin-bottom: 4px;
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 0.4px;
}
.field input,
.field select {
  width: 100%;
  padding: 7px 10px;
  font-size: 13px;
  border: 1.5px solid #e2e8f0;
  border-radius: 7px;
  background: #fafbfc;
  color: #1a2332;
  transition: border-color 0.15s;
}
.field input:focus,
.field select:focus {
  outline: none;
  border-color: #2563eb;
  background: white;
}

/* ---- JFT SECTION ---- */
.jft-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 10px;
}
@media (max-width: 600px) {
  .jft-grid { grid-template-columns: 1fr; }
}
.jft-unit {
  background: #f8fafc;
  border: 1px solid #e2e8f0;
  border-radius: 8px;
  padding: 10px 12px;
}
.jft-unit-title {
  font-size: 11px;
  font-weight: 600;
  color: #1a3a5c;
  margin-bottom: 8px;
}
.jft-row {
  display: grid;
  grid-template-columns: 1fr 80px;
  align-items: center;
  margin-bottom: 5px;
}
.jft-row label {
  font-size: 11px;
  color: #64748b;
}
.jft-row input {
  padding: 4px 8px;
  font-size: 13px;
  border: 1px solid #e2e8f0;
  border-radius: 5px;
  text-align: right;
  width: 100%;
  background: white;
}
.jft-row input:focus {
  outline: none;
  border-color: #2563eb;
}
.jft-total {
  font-size: 12px;
  font-weight: 600;
  color: #1a3a5c;
  padding-top: 6px;
  border-top: 1px solid #e2e8f0;
  margin-top: 4px;
  display: flex;
  justify-content: space-between;
}

/* ---- TABS ---- */
.tabs {
  display: flex;
  gap: 6px;
  margin-bottom: 12px;
  flex-wrap: wrap;
}
.tab {
  padding: 7px 16px;
  border-radius: 7px;
  border: 1.5px solid #e2e8f0;
  background: #fafbfc;
  color: #64748b;
  font-size: 13px;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.15s;
}
.tab:hover {
  border-color: #93c5fd;
  color: #2563eb;
}
.tab.active {
  background: #1a3a5c;
  color: white;
  border-color: #1a3a5c;
}

/* ---- STATUS BADGES ---- */
.status-row {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
  margin-bottom: 14px;
}
.badge {
  font-size: 11.5px;
  padding: 4px 11px;
  border-radius: 20px;
  border: 1.5px solid #e2e8f0;
  color: #94a3b8;
  background: #f8fafc;
  font-weight: 500;
  display: flex;
  align-items: center;
  gap: 5px;
}
.badge::before { content: '●'; font-size: 8px; }
.badge.ok {
  background: #f0fdf4;
  border-color: #86efac;
  color: #15803d;
}
.badge.ok::before { color: #22c55e; }

/* ---- PANEL & TEXTAREA ---- */
.panel { display: none; }
.panel.active { display: block; }

.textarea-label {
  font-size: 12px;
  color: #64748b;
  margin-bottom: 6px;
  display: block;
}
textarea {
  width: 100%;
  height: 210px;
  padding: 10px 12px;
  font-size: 12px;
  font-family: 'Courier New', monospace;
  border: 1.5px solid #e2e8f0;
  border-radius: 8px;
  background: #fafbfc;
  color: #1a2332;
  resize: vertical;
  line-height: 1.6;
  transition: border-color 0.15s;
}
textarea:focus {
  outline: none;
  border-color: #2563eb;
  background: white;
}
.hint {
  font-size: 11px;
  color: #94a3b8;
  margin-top: 6px;
  line-height: 1.5;
}

/* ---- PARSE PREVIEW ---- */
.parse-preview {
  margin-top: 10px;
  background: #f8fafc;
  border: 1px solid #e2e8f0;
  border-radius: 8px;
  padding: 10px 12px;
  display: none;
}
.parse-preview.show { display: block; }
.parse-title {
  font-size: 11px;
  font-weight: 600;
  color: #64748b;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  margin-bottom: 8px;
}
.parse-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(130px, 1fr));
  gap: 5px;
}
.parse-item {
  background: white;
  border: 1px solid #e2e8f0;
  border-radius: 5px;
  padding: 5px 8px;
}
.parse-item .key { font-size: 10px; color: #94a3b8; }
.parse-item .val { font-size: 13px; font-weight: 600; color: #1a3a5c; }
.parse-item.found .val { color: #15803d; }
.parse-item.zero  .val { color: #94a3b8; }

/* ---- BUTTONS ---- */
.btn-row {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}
.btn-gen {
  padding: 10px 24px;
  background: #1a3a5c;
  color: white;
  border: none;
  border-radius: 8px;
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.15s;
  letter-spacing: 0.3px;
}
.btn-gen:hover { background: #0d2540; }

.btn-clear {
  padding: 10px 16px;
  background: transparent;
  color: #64748b;
  border: 1.5px solid #e2e8f0;
  border-radius: 8px;
  font-size: 13px;
  cursor: pointer;
  transition: all 0.15s;
}
.btn-clear:hover { border-color: #94a3b8; color: #334155; }

.btn-copy {
  padding: 7px 16px;
  background: #15803d;
  color: white;
  border: none;
  border-radius: 7px;
  font-size: 13px;
  font-weight: 500;
  cursor: pointer;
  transition: background 0.15s;
}
.btn-copy:hover { background: #166534; }

/* ---- ERROR ---- */
.err {
  background: #fef2f2;
  border: 1px solid #fca5a5;
  color: #b91c1c;
  padding: 10px 14px;
  border-radius: 8px;
  font-size: 13px;
  margin-top: 10px;
  display: none;
}

/* ---- RESULT ---- */
.result-wrap { margin-top: 16px; }
.result-card {
  background: white;
  border-radius: 10px;
  box-shadow: 0 1px 4px rgba(0,0,0,0.07);
  overflow: hidden;
}
.result-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 12px 16px;
  background: #1a3a5c;
}
.result-label {
  font-size: 13px;
  font-weight: 600;
  color: white;
}
.result-body { padding: 16px; }
pre {
  font-size: 12.5px;
  font-family: 'Courier New', monospace;
  color: #1a2332;
  white-space: pre-wrap;
  word-break: break-word;
  line-height: 1.8;
}
